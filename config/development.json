{
  "environment": "development",
  "log_level": "DEBUG",
  "log_format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
  "repository_type": "in_memory",
  "storage": {
    "type": "json",
    "path": "data/meeting_rooms"
  },
  "logging": {
    "level": "DEBUG",
    "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    "handlers": [
      "console"
    ]
  }
}